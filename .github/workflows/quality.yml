name: Quality Gate

on:
  pull_request:
    branches: [main]
  push:
    branches: [main, fix/trade-journal-learning-rc4]
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: quality-${{ github.ref }}
  cancel-in-progress: true

jobs:
  rc4-source-patch:
    name: Apply RC4 trade pipeline patch
    if: (github.event_name == 'push' && github.ref == 'refs/heads/fix/trade-journal-learning-rc4' && github.actor != 'github-actions[bot]') || (github.event_name == 'pull_request' && github.head_ref == 'fix/trade-journal-learning-rc4')
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/trade-journal-learning-rc4
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Apply source patch
        run: node scripts/rc4-trade-pipeline-fix.mjs
      - name: Commit source changes
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add electron frontend shared tests package.json package-lock.json .github/workflows/windows-installer.yml
          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git commit -m "fix: restore trade journal counting and verified learning"
          git push origin HEAD:fix/trade-journal-learning-rc4

  ipc-contract:
    name: IPC contract audit
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run verify:ipc

  tests:
    name: Vitest suite
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Run Vitest
        id: vitest
        shell: bash
        run: |
          set +e
          npm run test 2>&1 | tee vitest-output.txt
          test_status=${PIPESTATUS[0]}
          echo "status=${test_status}" >> "$GITHUB_OUTPUT"
          exit "${test_status}"
      - name: Publish Vitest failure summary
        if: failure() && github.event_name == 'pull_request'
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        shell: bash
        run: |
          {
            echo '<!-- mars-vitest-failure -->'
            echo '### Vitest failure summary'
            echo
            echo "Commit: \`${GITHUB_SHA}\`"
            echo
            echo '<details><summary>Last 180 lines</summary>'
            echo
            echo '```text'
            tail -n 180 vitest-output.txt
            echo '```'
            echo '</details>'
          } > vitest-comment.md
          gh pr comment "$PR_NUMBER" --body-file vitest-comment.md

  typecheck:
    name: Electron and frontend typecheck
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run typecheck

  build:
    name: Production build verification
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run build
